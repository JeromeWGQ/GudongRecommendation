<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lrving.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ProfileActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AgeActivity" />
        <activity android:name=".HobbiesActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".AutographActivity" />
        <activity android:name=".GoalActivity" />
        <activity android:name=".HeightActivity" />
        <activity android:name=".NameActivity" />
        <activity android:name=".SexActivity"></activity>
    </application>

</manifest>